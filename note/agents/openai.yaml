interface:
  display_name: "Note"
  short_description: "Add durable notes to agent instructions"
  default_prompt: "Use $note to turn this note into durable AGENTS.md guidance."
policy:
  allow_implicit_invocation: false
