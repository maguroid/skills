interface:
  display_name: "Harness Sync"
  short_description: "Sync dotfiles, mise, Tailscale, skills, and hubs safely"
  default_prompt: "Use $harness-sync to sync or diagnose this machine while keeping credentials and Tailscale state machine-local."
