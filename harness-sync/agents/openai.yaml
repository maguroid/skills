interface:
  display_name: "Harness Sync"
  short_description: "Sync dotfiles, apps, tools, skills, and hubs safely"
  default_prompt: "Use $harness-sync to sync or diagnose this Mac, including Homebrew Casks such as Hammerspoon and the standalone Codex CLI, while keeping credentials and macOS privacy approvals machine-local."
